<div class="container">
  <div class="row row-cols-1 row-cols-md-3">
    <% @posts.each do |post| %>
     <div class="col"> 
      <div class="card"> 
        <%= image_tag post.image, class: "card-img-top"%>
        <div class="card-body">
          <h5 class="text-center"><%=safe_join(post.title.split("\n"),tag(:br))%></h5>
          <p class="text-center"><%= post.text.truncate(20) %></p>
          <div class="text-center">
          <p class="text-center">投稿者<%=post.user.nickname%></p>
          いいね♥<%= post.likes.count%>
            <%=link_to '続きを読む', post_path(post.id), class: "btn btn-outline-success"%>
          </div>
        </div>
      </div>
    </div> 
  <% end %>
 </div>